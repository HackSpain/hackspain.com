---
export const prerender = false;

const destination = new URL("/cancelacion", Astro.url);
destination.search = Astro.url.search;

return Astro.redirect(destination.toString(), 308);
---

<!-- Legacy route kept so previously issued links continue to work. -->